name: Deploy Man Cave TV to Pages

# This tells the action to run whenever you push to the 'main' branch
# or when you manually click 'Run workflow' in the Actions tab
on:
  push:
    branches: ["main"]
  workflow_dispatch:

# Grants the action permission to write to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Prevents multiple deployments from running at the same time
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository
        uses: actions/checkout@v4

      - name: Prepare static site and registry sections
        run: python3 tools/prepare_site.py

      - name: Check player and navigator
        run: node --test tests/site.test.cjs

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload the prepared site
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
